##name_matching

Check Names in two columns and compare them and tells you how similar or otherwise they matched using fuzzy match.

This script extracts and matches names using fuzzywuzy and pandas in matching two columns with names and give the percentage of how they matched. This can be used to compare two columns of text to see how similar or otherwise they are.
